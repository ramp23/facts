apiVersion: v1
kind: Service
metadata:
  name: {{ include "facts.fullname" . }}
  labels:
    {{- include "facts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "facts.selectorLabels" . | nindent 4 }}
